<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorTest">

    <include
        android:id="@+id/loader"
        layout="@layout/layout_loader" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="@dimen/padding_common_large"
        android:gravity="center"
        android:text="User not registered yet in Karma, but you can leave comment for him right now"
        android:textSize="20sp" />

    <com.smedialink.tokenplussteamid.views.InputCommentView
        android:id="@+id/field_comment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom" />

</FrameLayout>